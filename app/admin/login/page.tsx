"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminLoginPage(){const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const res=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const json=await res.json().catch(()=>({}));if(!res.ok){setError(json.error||"Could not sign in.");return}router.push("/admin");router.refresh()}catch{setError("Could not sign in. Please try again.")}finally{setBusy(false)}}return <main className="page-shell grid min-h-[calc(100vh-64px)] place-items-center py-10"><div className="w-full max-w-md"><div className="surface overflow-hidden"><div className="bg-[linear-gradient(135deg,#102a2b,#0f6b69)] px-6 py-8 text-white sm:px-8"><p className="eyebrow">Officer workspace</p><h1 className="mt-3 text-white">Welcome back.</h1><p className="mt-3 max-w-sm text-sm leading-relaxed text-white/70">A focused workspace for reading the town pulse, managing destinations, and keeping visitor feedback useful.</p></div><form onSubmit={submit} className="p-6 sm:p-8"><label className="block text-[10px] font-extrabold uppercase tracking-[.14em] text-[var(--muted)]">Desk password</label><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-2xl border border-[var(--line)] bg-white px-4 py-3 text-sm outline-none focus:border-[var(--teal)] focus:ring-4 focus:ring-[rgba(15,107,105,.08)]"/>{error&&<p className="mt-2 text-xs text-[var(--coral)]">{error}</p>}<button disabled={busy} className="btn-primary mt-4 w-full">{busy?"Verifying…":"Enter officer desk →"}</button><p className="mt-4 text-center text-[10px] text-[var(--muted)]">Visitors never use this sign-in.</p></form></div></div></main>}
