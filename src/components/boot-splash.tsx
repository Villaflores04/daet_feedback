"use client";
import type { ReactNode } from "react";
/** Content renders immediately; the coastal hero provides the welcome experience. */
export function BootSplash({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
